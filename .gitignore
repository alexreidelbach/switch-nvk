# Big / reconstructable trees — NOT versioned (regenerated from upstream + apply-patches.sh).
mesa-25/
mesa/
devkitpro-mesa/
libdrm_nouveau/
libc-switch/

# Build outputs / generated.
mb/
native-prefix/
native-tools/
sysroot/
crossfiles/
/*.nro
winsys/logs/
winsys/*.nro
winsys/*.txt
winsys/smoke/shaders/*.spv

# Logs.
*.log

# RE of Dan's PUBLIC release binary — keep dan-re/*.md notes, ignore the binary + Ghidra/JDK + scratch.
dan-re/*.nro
dan-re/tools/
dan-re/ghidra_proj/
dan-re/_*.txt

# Keep: Dockerfile, *.sh, *.json, *.md, compat/, patches/, pristine-25.0.7/, UNDEFINED_SYMBOLS.txt
