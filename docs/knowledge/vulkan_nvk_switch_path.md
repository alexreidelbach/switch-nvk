---
name: vulkan-nvk-switch-path
description: "The Vulkan-on-Switch path — only missing piece is Dan's NVK driver (private Docker image); everything else wired"
metadata: 
  node_type: memory
  type: project
  originSessionId: 15fe3c02-602e-4f2e-ab84-754a6a065183
---

**🎉🎉🎉🎉🎉 M3 GRAPHICS WORKING ON REAL TEGRA (2026-05-27) — triangle, textures, 3D textured cube @60fps, DEPTH, + Sascha's real shaders. Read this first.**
On the GM20B, all rendered by our own NVK and shown on the TV (libnx-framebuffer blit of NVK-rendered images): present (blue clear) → drawn triangle (NAK shaders) → textured quad ("VULKAN" logo, sampler+descriptor) → 3D textured cube (vertex buffer + MVP UBO + descriptor) → **spinning at vsync 60 fps** → **3D cube WITH a real depth buffer** → and a **POC running SaschaWillems/Vulkan's VERBATIM `triangle.vert/.frag`** (MIT) compiled by our NAK = the canonical RGB Vulkan triangle, established PC code on our driver. Apps: `winsys/smoke/{nvk_tri,nvk_logo,nvk_scene,nvk_poc}.c`; shaders `winsys/smoke/shaders/*.{vert,frag}` → `gen-shaders.sh`(glslangValidator) → `tri_shaders.h`; `build-nro.sh` takes `APP/TITLE/VERSION`.
- **DEPTH ✅ RESOLVED — the universal 3D blocker, now fix-once.** Root cause (ground-truth-confirmed by instrumenting NIL): `src/vulkan/runtime/vk_image.c:99` sets `drm_format_mod=DRM_FORMAT_MOD_INVALID` only `#if DETECT_OS_LINUX||DETECT_OS_BSD`; our **HORIZON** port fell outside → it stayed `0`=`MOD_LINEAR` → NIL forced EVERY image LINEAR (`gob=0,pte_kind=0`) → GM20B ZETA can't clear linear depth → GR fault on `CLEAR_SURFACE`(Z) (mthd 0x19d0). **Fix (2 parts, both in the repo): (1) add `DETECT_OS_HORIZON` to that vk_image.c guard (folded into the patch); (2) `drm_shim.c vm_bind_op` must take the PTE kind from `op->flags & 0xff` (NVK puts the kind there for the new uAPI, NOT in GEM_NEW tile_flags which is 0 for OPTIMAL).** Confirmed on HW: depth `pte_kind=0x7b`(ZF32), `gob=1`, VM_BIND `opflags=0x7b`, no ERRNOTIF, cube renders. **Unblocks ALL 3D-with-depth content + Dawn-over-Vulkan GC/Wii ports.** Lesson: code-reading said "0x7b" but HW said "0" — only instrumenting (the `mod=0x0` ground-truth) broke the deadlock. [[nvk-winsys-debugging-patterns]]
- **60fps lesson:** `DRM_SHIM_DEBUG` (per-frame SD-card `fflush`) throttled the spin to ~10fps with bursty stutter; build WITHOUT it for real-time. **Exit lesson:** framebuffer apps fault on process teardown (recursive User Break, libnx/display vs NVK nv); for a POC photo it's irrelevant. **Present = libnx framebuffer blit, NOT Vulkan WSI yet.**
- **NEXT:** the `texturecubemap` POC (cubemap: 6-layer CUBE image + samplerCube + Sascha's skybox shaders, already saved as `sascha_skybox.{vert,frag}`); then real WSI. Repo PRIVATE `github.com/HayatoG/switch-nvk`.

**🎉🎉🎉 M3 TRIANGLE (2026-05-27, t1b) — earlier milestone (superseded by the block above).**
A YELLOW TRIANGLE rendered by our own NVK and displayed on the Switch screen. Path: `vkCreateDevice=0 → image+view+renderpass+framebuffer → vkCreateShaderModule(vert+frag) → vkCreateGraphicsPipelines=0 → renderpass(clear black)+vkCmdDraw(3) → copy image→buffer → readback yellow=722 black=3374 other=0 center=0xff00ffff → present`. **Proves the last graphics unknowns on HW: NAK-compiled vertex+fragment shaders EXECUTE on the GM20B; pipeline+renderpass+rasterisation work; result shown on the TV.** This is the classic "Vulkan triangle" = Dan's first-artifact goal, on our from-scratch winsys. M3 essentially done.
- **App** `winsys/smoke/nvk_tri.c` (`APP=nvk_tri`); shaders `winsys/smoke/shaders/triangle.{vert,frag}` → `gen-shaders.sh` (glslangValidator, added to the Docker image) → embedded `tri_shaders.h`. **Present** = `present_shot()`: upscales the rendered 64×64 image to 1280×720 via the **libnx framebuffer** (NOT Vulkan WSI yet), shown until `+`, done AFTER Vulkan teardown (avoids GPU/display contention).
- **Steps:** 1a = clear image→readback→present (blue on TV); 1b = draw triangle (shaders)→present (yellow triangle on TV). Both headless-verifiable + on-screen.
- **NEXT (the "art placeholder"):** a TEXTURED quad of the **Vulkan "Industry Forged" logo** — one standard increment above the triangle (sampled texture upload via staging+copy [proven] + sampler + descriptor set + textured frag shader). Optional polish: real Vulkan WSI swapchain over `nwindow` (ref `dantiicu/vulkan-triangle-test-switch`).
- **REPO:** all pushed PRIVATE to `github.com/HayatoG/switch-nvk` (winsys + docs/knowledge + BUILD_AND_RUN.md). State: `D:\switch-nvk\RESUME_NVK.md`. Patterns: [[nvk-winsys-debugging-patterns]].

**🎉🎉🎉 M2 SMOKE TEST PASSED ON REAL TEGRA (2026-05-27, v32). (superseded by M3 above)**
`I VERIFY OK: all 1024 words == 0xcafebabe / SMOKE TEST PASSED — NVK rendered to memory on Tegra`. Full headless Vulkan runs on GM20B: vkCreateInstance→enumerate→**vkCreateDevice=0**→buffer/mem/map/bind→vkCmdFillBuffer(0xCAFEBABE)→**vkQueueSubmit=0**→**vkQueueWaitIdle=0**→**CPU readback==0xCAFEBABE**. The ENTIRE M2 winsys is validated on HW (device, VM_BIND, GPFIFO submit, fence signalling, GPU execution, CPU/GPU coherency).
- **THE FIX (v32):** after `nvGpuChannelIncrFence`, **append the builtin FENCE CMDLIST** (`nvGpuChannelAppendEntry(ch->cmdbuf_va, ch->fence_num_cmds, NOT_MAIN|NO_PREFETCH)`) — mirrors `libdrm_nouveau/pushbuf.c:226`. IncrFence alone only bumps libnx's counter; the GPU only increments the syncpt when this cmdlist RUNS. The v22 removal of it was the bug (made on the misread 0xd5c=ENOMEM; it's Timeout). Init was masked (NVK self-increments); the fill stalled (syncpt frozen) until this.
- **KEY (research):** the fence cmdlist's 3rd dword = `syncpt_id | (1<<20) | (1<<16)` — **bit20=syncpt incr, bit16=GPU L2 FLUSH**. One cmdlist fixed BOTH completion (drain rc=0x0, reached=1) AND coherency (L2 flush → CPU sees 0xCAFEBABE, no non-cacheable map needed). v31's `-1` hack REVERTED (only worked for init by coincidence).
- **Settled by deep research (local Mesa 20 `libdrm_nouveau`+`devkitpro-mesa` + NVK source):** GM20B is fully usable with **ZERO FECS/priv-reg writes** (Mesa 20 proves it via `nvc0_magic_3d_init`, pure 0xB197 methods) — our v28 no-op is correct; both init priv-writes are non-essential, the conservative-raster one is lazy/draw-time. NVK self-uploads its full 3D golden state + MME macros (no kernel golden context needed); it relies on the winsys to SIGNAL the sig-syncobj on push-retire (we now do). Engine bind = in-stream SET_OBJECT (subch 3D=0,CP=1,M2MF=2,2D=3,COPY=4).
- **NEXT = M3 (the triangle + present):** real vkCmdDraw into a VkImage + WSI over libnx `nwindow`/`vi` (ref `dantiicu/vulkan-triangle-test-switch`). When cross-submit reads appear, add the **flush cmdlist + NO_PREFETCH NOP barrier after kickoff** (`pushbuf.c:255-264`) for inter-submit coherency. Resume web-research agent `ac4f935a22fcb4362` (hit session limit) for GM20B/WSI gotchas. All state: `D:\switch-nvk\RESUME_NVK.md`. Patterns: [[nvk-winsys-debugging-patterns]]. ⚠️ The `nvk_cmd_draw.c` FECS no-op is a working-tree edit — fold into the patch file before re-extract.

---

**⭐⭐⭐⭐ (SUPERSEDED by the PASS above) STATE 2026-05-27 — PAST THE FECS WALL; init EXEC now HANGS.**
- **`0xd5c` = `MAKERESULT(348,6)` = `LibnxNvidiaError_Timeout`** (channel was RESET), NOT InsufficientMemory (that's desc 7=`0xf5c`). The whole-session "memory pressure / num_inflight_jobs / warmup ramp" reasoning was built on the WRONG error reading — DISREGARD it. WARMUP RAMP is REVERTED (heavy flushes wedged the console).
- **ROOT CAUSE of the fill failure (FOUND, NOT coherency):** NVK's 3D init calls `nvk_mme_set_priv_reg` (in `mesa-25/src/nouveau/vulkan/nvk_cmd_draw.c`) → `SET_FALCON04` (method 0x2310, class 0xb197) to write **privileged GR regs via the FECS falcon** (clears bit3 of `sm_disp_ctrl` 0x419f78 + bit14 of `warp_esr_report_mask` 0x419e44 — both NON-essential robustness tweaks per NVK comments). **Horizon BLOCKS homebrew FECS priv writes** → FECS error (`GetErrorInfo` type=2 GR, info[0]=0x80000 FECS bit, info[1]=0x102310 trapped method, info[4]=0xb197) → MMU-fault notifier (`ERRNOTIF type=31`) → channel reset → every later submit Timeouts `0xd5c`. The `drain rc=0x0 fence=27/402` were FALSE POSITIVES (recovery force-advances the syncpt).
- **v28 FIX (applied + PROVEN active on real HW):** patched `nvk_mme_set_priv_reg` to consume its 3 inline args and **skip `mme_set_priv_reg` entirely** (no SET_FALCON04, no FECS wait-loop). Confirmed: init pushbuf SHRANK `0x1d0c→0x1cd0`, and across multiple HW runs **NO `ERRNOTIF type=31`, NO `kickoff failed`** (the FECS fault used to be reliable here), no console wedge/reboot. **We are PAST the FECS wall.**
- **NEW BLOCKER (v28 on HW):** the init EXEC now RUNS but its **3D GPU work never COMPLETES** — log freezes right after `EXEC push content`, no `EXEC drain` line → the init's end-of-submit syncpt increment never fires → our drain's `nvFenceWait` blocks. It LOOKED like an infinite hang (3+min black screen) due to a **TIMEOUT-UNITS BUG: `nvFenceWait` takes MICROSECONDS, but our drain passed `2000000000` ("2s") = 2000s ≈ 33min.**
- **v29 (CODE STAGED in `winsys/drm_shim.c`, NOT YET BUILT):** drain `nvFenceWait` `2e9→2e6`(=2s); wait-syncs `-1→2e6`(=2s); `drmSyncobjWait` already capped 3s; added `EXEC kickoff returned rc=0x%x` log before the drain. BUILD tag `v29 bounded-waits`, nacp `0.29.0-boundedwaits`. **This won't make it PASS — it makes the app FINISH (not hang 33min) so we get the FIRST COMPLETE log** (does the init drain time out? does NVK reach the fill? late fault?).
- **NEXT (user's call: STUDY, like the FECS dig — do NOT blind-iterate):** why does the init's GPU work hang now? (a) a priv-reg I skipped is actually needed for init to complete, or (b) the init runs PAST FECS and hits a new stall (WFI/semaphore-acquire). Study NVK's full device-init pushbuf sequence (what runs after the priv-reg writes) + how the native GM20B GL stack (devkitPro Mesa 20 / `libdrm_nouveau`, cloned locally) completes init without FECS priv writes. The v29 complete log informs this. Standing rule [[feedback-nvk-read-full-log]]: read the WHOLE log every run. Upload to `sdmc:/switch/` (favorited path), launch via Sphaira "boot as application", poll `ftp://<ip>:5000/sdmc:/nvk_smoke.log`.

---

**Vulkan on Switch (HOS homebrew) — the path, per Dan's hints + repo investigation (2026-05-25):**
Dan's hints: "understand NVK, understand nwindow, libnx, build a WSI, you are done." Decoded:

- **WSI** = Window System Integration: VkSurface + VkSwapchain + present → libnx `nwindow` (via `vi`
  service). It's the glue between the Vulkan driver and the Switch display.
- The ONLY missing piece for us is the **NVK driver build**. Everything else is public or already wired:
  - **NVK driver** = `ticohq/switch-nvk-vulkan` Docker image (NVK installed at `/opt/nvk-switch`,
    linked via `pkg-config nvk-switch-vulkan`). Built from a Mesa fork via `nvk-image/create-nvk-image.sh /path/to/mesa`. **The image is PRIVATE** (Docker Hub: denied/object-not-found). Dan's `nvk-image` build setup is NOT among his public repos. So the NVK build = the one private piece.
  - **WSI** = PUBLIC in `github.com/dantiicu/vulkan-triangle-test-switch` (`TriangleTest.cpp`: VI surface + swapchain) and his `vulkan-smoke/compute-test-switch`. Reference it.
  - **Our build is ALREADY wired for it:** `extern/aurora/cmake/aurora_core.cmake` Switch-non-deko-non-gles branch sets `DAWN_SWITCH_NVK_ROOT=/opt/nvk-switch` + `DAWN_SWITCH_NVK_LIBRARY=/opt/nvk-switch/lib/libvulkan.a` and `FATAL_ERROR`s if absent. `aurora-switch` (Dan's) builds Dawn with `DAWN_ENABLE_VULKAN=ON` on Switch.

**Highest-leverage action: ask Dan to share `ticohq/switch-nvk-vulkan`** (make public / grant pull),
OR the Mesa fork + `nvk-image/create-nvk-image.sh`. Then: pull image → `/opt/nvk-switch` → build with
the Vulkan backend (our cmake is ready) + the public WSI → Dawn-Vulkan-NVK = Dan's exact result at
native perf. Without Dan: porting NVK to GM20B (Tegra X1 integrated Maxwell, unsupported upstream) +
the Switch `nv` services ourselves = weeks of GPU-driver work.

**Dan's method (told to user 2026-05-25, confirms our approach):** "no Rust toolchain for the Switch — configured one by hand, with **std all shimmed** (NVK's compiler NAK is Rust)." → exactly our M0 std-port (shim std::sys over libnx). He also **ported Mesa version-by-version from 20.13 → 21.0 → ...** handling each version's Switch breaking-changes (~1.5 months, AI-heavy, "humanly impossible to understand the whole structure" in that time). Implication for OUR M1 (the C/winsys port): don't use bleeding-edge Mesa (we cloned 26.2-devel = all breaking changes at once) nor 20.13 (pre-NVK; NVK landed Mesa ~24.0) — **pin to a stable NVK-era Mesa (24.x/25.x)** where NVK is Maxwell-conformant but the code is closer to portable. The Rust std port is version-INDEPENDENT (it's the toolchain) so it carries over. **M0 STD DONE (2026-05-25):** Rust std builds AND links a binary for the Switch. Recipe (all in `D:\switch-nvk\`): toolchain image `switch-nvk-build` (devkitA64 + meson + rust nightly + rust-src + bindgen); custom target `D:\switch-nvk\aarch64-switch-horizon.json` = the built-in `aarch64-nintendo-switch-freestanding` spec + `"target-family":["unix"]` + `"env":"newlib"` (this one change cascaded 45 build-std errors → 0 by routing libc+std to the unix/newlib path — exactly Dan's "std shimmed"); build with `RUST_TARGET_PATH=/work RUSTC_BOOTSTRAP=1 RUSTFLAGS="-Zunstable-options -L /opt/devkitpro/devkitA64/aarch64-none-elf/lib -L /opt/devkitpro/libnx/lib"` + `cargo +nightly build -Zbuild-std=core,alloc,std,panic_abort --target aarch64-switch-horizon`. (RUSTC_BOOTSTRAP+`-Zunstable-options` to load the custom JSON; the -L paths give newlib libc/libm.) This is the Rust toolchain NAK needs at NVK-era.

**PLAN PIVOT (user, 2026-05-25): forward-port from the EXISTING Switch Mesa, don't write from scratch.** The Switch↔nv winsys ALREADY exists publicly: **`devkitPro/mesa`** ("Switch port of Mesa 3D", Mesa 20.1, the GL/nouveau driver that runs on the GM20B today = `switch-mesa`) + **`devkitPro/libdrm_nouveau`** (libdrm nouveau backed by libnx nv services). Cloned to `D:\switch-nvk\devkitpro-mesa` + `libdrm_nouveau`. New M0/M1: base on these (known-good, always-buildable), forward-port version-by-version toward NVK-era (Mesa ~24.0, where NVK/NAK land + the std toolchain plugs in), inheriting/adapting the Switch nv integration. Mirrors Dan's 20.13→up method. The std work (done) converges at the top.

**M1-BUILD DONE — NVK FULLY CROSS-COMPILES FOR THE SWITCH (2026-05-25):** all 837 build steps pass; the static libs build (`libnvk.a`, vulkan runtime/util/wsi, NAK+NIL Rust). The final ICD `.so` link is NOT our target (it also needs Switch-app linker-script syms `__tls_*`/`main`) — we link the static `.a`s into a Switch EXE with `switch.ld`. **The remaining link undefineds ARE the exact M1/M2 spec** (saved to `D:\switch-nvk\UNDEFINED_SYMBOLS.txt`): (A) winsys/DRM — `drmGetVersion/FreeVersion/FreeDevice/GetDeviceFromDevId/CommandWrite/CommandWriteRead/CloseBufferHandle/PrimeFDToHandle/PrimeHandleToFD/SyncobjCreate/Destroy/Wait` + `vk_drm_syncobj_*` (→ thin `drm_shim.c` over libnx nv, ref `D:\switch-nvk\libdrm_nouveau`); (B) app linker-script syms (provided by devkitA64 `switch.ld`+crt0 at EXE link, not shims); (C) libc/std gaps — `secure_getenv`(have compat.c), `mmap/munmap`(compat.c), `getrandom`(→libnx randomGet), `flock/pthread_sigmask/sysconf/regcomp/posix_memalign/pread/pwrite` + Rust-std unix creds (→ `switch_libc_shim.c`). **KEY CROSS-BUILD INSIGHT: `mesa-clc=enabled` in a cross build wrongly compiles LLVM/CLC FOR the target — use the 2-phase recipe: native `mesa_clc`+precomp (`build-native-tools.sh`) then cross `-Dmesa-clc=system -Dprecomp-compiler=system -Dllvm=disabled`.** **EVERYTHING IS REPRODUCIBLE + VERSIONED: `D:\switch-nvk` is now a git repo; the 6 Mesa source patches are captured authoritatively in `patches/switch-nvk-mesa-25.0.7.patch` (verified to round-trip from pristine 25.0.7) + re-applied by `apply-patches.sh` (idempotent); full state/recipe/inventory in `D:\switch-nvk\RESUME_NVK.md` — READ THAT FIRST next session.** See [[dan-nvk-intel-and-goal]].

**⭐ AUTONOMOUS EDEN DEBUG LOOP + ENUMERATION WORKING (2026-05-26):** HUGE velocity unlock — run the NVK smoke `.nro` HEADLESS in **Eden** and read the log directly, NO Switch/FTP/user. Eden at `D:\Eden-Windows-v0.2.0-amd64-gcc-standard\eden-cli.exe`; sdmc at `%APPDATA%\Eden\sdmc\`. Loop: `docker ... build-nro.sh` → `Start-Process eden-cli.exe "D:\switch-nvk\nvk_smoke.nro"` (~70s, kill after) → read `%APPDATA%\Eden\sdmc\nvk_smoke.log`. Eden reproduces ALL CPU/enumeration logic; build/link bugs verify even faster via `nm` (no run). Two real bugs found+fixed autonomously (NOT HW issues): **(1)** `mesa-25/src/util/libdrm.h` shipped `-ENOENT` STUB `drm*` inline funcs when `!HAVE_LIBDRM` — so `vk_instance.c`'s `enumerate_drm_physical_devices_locked` called the stub `drmGetDevices2`(→ -ENOENT), never our shim → 0 devices. FIX: `#if defined(HAVE_LIBDRM)||defined(__SWITCH__)` → real `<xf86drm.h>` (now the 8th Mesa patch). Diagnosed via a function-ADDRESS comparison log (app's `&drmGetDevices2` ≠ vk_instance's → two symbols → stub). **(2)** our shim's `drmGetDeviceFromDevId` was a `-ENODEV` stub; `nvkmd_nouveau_create_dev` calls it → `vkCreateDevice` -3. FIX: return synthetic GM20B; also added `stat`/`lstat` `--wrap` (NVK `stat()`s the render node for dev_t). **RESULT: `vkEnumeratePhysicalDevices` → 1 device "NVIDIA Tegra X1 (NVK GM20B)" (api 1.3.305, vendor 0x10de, INTEGRATED, qfam0=gfx+compute+transfer+sparse) — ENUMERATION FULLY WORKS in Eden.** Then `vkCreateDevice` creates real channels + allocs BOs and **CRASHES right after `GEM_NEW handle=3`** on the next GPU op (mmap/VM_BIND/EXEC) — M2's real GPU path, likely beyond Eden's `nv` emulation (intel: NVK needs real HW). META lessons: Docker bind-mount mtimes make ninja SKIP edited Mesa files → delete the `.o` to force recompile; `libnvk.a` is a THIN archive (`strings` won't show code, `nm` follows it); `MESA_LOG_FILE=sdmc:/x.log` to capture NVK's `vk_errorf`; Eden doesn't forward guest stdout (use sdmc file logs). Smoke test (`winsys/smoke/nvk_smoke.c`) has a drm_shim selftest + a `g_drm_shim_log_sink` hook routing shim trace into the app log. **NEXT: SHIM_LOG `drm_shim_mmap`/`vm_bind_op` entry to pinpoint the GEM#3 crash; decide Eden-limit vs shim bug; submit smoke on real Tegra.**

**⭐ M2 ON REAL TEGRA: enumeration→device→VM_BIND all WORKING; only GPFIFO submit left (2026-05-26).** Via the autonomous Eden loop + HW iteration (deep research agents), peeled the M2 GPU layers on real hardware:
- **vkCreateInstance ✅, vkEnumeratePhysicalDevices ✅** (GM20B "NVIDIA Tegra X1 (NVK GM20B)", after the `util/libdrm.h` HAVE_LIBDRM-stub fix + `drmGetDeviceFromDevId`→synthetic device + `stat`/`lstat` `--wrap`).
- **vkCreateDevice gets deep; VM_BIND (GPU-VA mapping) ✅ WORKS on HW** — this was the hardest blocker. ROOT CAUSE (research, sourced): Tegra nvgpu splits each channel's VA into a **low/small-page (4KB) half `[low_hole,16GiB)`** and a **high/big-page (128KB) half `[16GiB,32GiB)`**; the page size of a FIXED map is FORCED by the address half; and **bare fixed-VA reservation is rejected (-EINVAL/0x275c)** — a FIXED map is only valid INSIDE a region previously reserved NON-fixed (kernel `validate_fixed_buffer`). FIX (deko3d's pattern): the shim reserves ONE big small-page arena non-fixed at nv-up (`nvioctlNvhostAsGpu_AllocSpace(pages, 0x1000, /*non-fixed*/0, &base)`; `drm_shim_va_base()`), and a **patched `nvkmd_nouveau_create_dev`** points NVK's VA heap [util_vma_heap] entirely inside that arena (`dev->base.va_start/va_end` + both heaps). Then `vm_bind_op` is just `MapBufferEx FIXED page=0x1000` inside the arena (no per-bind AllocSpace). NVK's heap was `[4KiB,1<<38]` (256GiB, top→0x3ffffff000) — outside nvgpu's range; needed `[arena_base, +8GiB]` (2GiB was too little → `-2` OUT_OF_DEVICE_MEMORY; 8GiB ok). Eden FAKED fixed-VA so it diverges here — **GPU VA/submit only validate on real Tegra.**
- **GPFIFO submit (EXEC/kickoff): FIXED structurally — full pipeline runs in Eden (2026-05-26).** The kickoff `rc=0xd5c=NvError_InsufficientMemory` was a **DOUBLE syncpoint increment**: `nouveau_exec` appended an in-stream fence cmdlist (gen_fence_cmdlist, +1) AND called `nvGpuChannelIncrFence` → KickoffPb `BIT(8)` → the KERNEL also injects an increment via `priv_cmd_q` (tiny because libnx hardcodes `AllocGpfifoEx2 num_jobs=1`) → `gk20a_submit_prepare_syncs` ENOMEM on the first fenced submit. FIX: drop `nvGpuChannelIncrFence` (in-stream increment only, no BIT(8) → kernel never touches priv_cmd_q); build the completion fence manually as `GetFence().value + 1`. After this, **Eden runs the ENTIRE smoke pipeline clean: vkCreateInstance→device→mem→buffer→cmdbuf→`vkQueueSubmit`→`vkQueueWaitIdle` all → 0.** (deko confirms: only the 3D obj-ctx is kernel-allocated; compute/copy/2d/m2mf bind via in-stream `SetObject` — `AllocObjCtx` for them is -EINVAL, don't chase it.)
- **REMAINING: the actual GPU fill must execute on real Tegra.** In Eden the readback is `VERIFY FAIL: all words 0x00000000` — EXPECTED: Eden accepts the submit but does NOT execute the arbitrary Maxwell GPFIFO stream (it's built for nvn/deko games via translation), so `vkCmdFillBuffer(0xCAFEBABE)` never runs → memory stays zero. **Only real Tegra executes the command stream → readback should show 0xCAFEBABE = PASS.** HW run pending (user). If HW kickoff still ENOMEMs, agent's backups: bump `num_jobs` (call AllocGpfifoEx2 directly), and verify push-VA residency / page-size (A/B: submit a tiny cmdlist in the already-mapped builtin cmdbuf). META: `vm_bind`/`vm_init` page size must be 4KB (small-page half); `NvError 6`≠Linux ENOMEM(12); shim error decode `0x__c`→module 348 LibnxNvidia, `>>9`=nv desc.

**⭐ EXEC `0xd5c` ON REAL TEGRA = NVK PUSHBUF *CONTENT*, NOT channel/size (2026-05-26 cont):** Iterated the kickoff failure on HW via the FTP-log loop (nxlink netloader is FLAKY — Stop-Job leaves the Switch netloader wedged → `Connection failed` on re-arm; **switched to: FTP-upload the `.nro` + user launches via Sphaira file browser + poll `ftp://IP:5000/nvk_smoke.log`** — reliable, no netloader). Decisive selftests in `drm_shim_selftest` + `nouveau_exec` (all on real Tegra): **T1** bigpage builtin-cmdbuf kickoff=0x0; **T2** small-page arena BO (MapBufferEx FIXED) kickoff=0x0; **T3** LARGE IB (1857 dwords of repeated fence cmdlists in the arena) kickoff=**0x0** → **IB SIZE is NOT the cause**; **EXEC probe** (trivial fence cmdlist on NVK's OWN channel) kickoff=**0x0** → **NVK's channel is FINE**. Only NVK's real init pushbuf fails: `EXEC push[0] va=… len=0x1d0c (1859 dw) in_arena=1` → kickoff `0xd5c`. Content peek of NVK's pushbuf head: `20010000 0000b197 …` = `SetObject(subch0)=0xB197 MAXWELL_B` then 3D-init methods — **normal leading content, nothing obviously forbidden**. So the blocker is a **SPECIFIC METHOD mid-stream** that Horizon's nvgpu rejects synchronously with InsufficientMemory (NOT a hard "invalid gpfifo data" terminate → likely a kernel-side resource alloc, e.g. MME microcode upload / a semaphore/report method / a privileged reg). **NEXT: prefix-scan** (added to `nouveau_exec`, first EXEC: submit growing prefixes {16,64,128,256,512,1024,1536,full} of NVK's push, log each kickoff rc; first `0x0→0xd5c` flip brackets the offending method) → then read the dwords at that boundary → sanitize/avoid it in the shim.

**⭐ DAN'S PUBLIC REPOS DON'T CONTAIN THE FIX — it's in his PRIVATE Mesa fork (2026-05-26 cont):** Investigated all of Dan's public repos for a submit-path hint. **`github.com/dantiicu`** = 7 repos: `aurora-switch`, `dawn-switch`, `Proton`, `website`, and **3 NVK app tests**: `vulkan-triangle-test-switch`, `vulkan-compute-shader-test-switch`, and a NEW one **`vulkan-smoke-test-switch`** (`SmokeTest.cpp`+`build.sh`, near-identical to ours: instance→device→surface→swapchain→submit→present). **`github.com/ticohq`** = 16 repos, ALL emulator forks (tico = a Switch emulation frontend: tico-dolphin/ppsspp/flycast/… ) — **NO Mesa fork, NO nvk-image, NO winsys source.** Key facts learned: (1) Dan's `build.sh` links the app against a PREBUILT NVK via `pkg-config` from the Docker image `ticohq/switch-nvk-vulkan` (`/opt/nvk-switch`), built by `nvk-image/create-nvk-image.sh /path/to/mesa` — **his Mesa fork = the one private piece, baked into the image**. (2) His `build.sh` uses **NO `--wrap` linker flags** → his Mesa fork patches **nvkmd/nouveau to call libnx `nv` DIRECTLY** (no libdrm/`open()` render-node path), unlike our drm_shim-over-libc-wrap. The submit fix lives in that private nvkmd. (3) His `SmokeTest.cpp` sets `__nx_heap_size=0` with comment *"NVK channel initialization exceeds the applet pool during bring-up"* — **we already do this** (Application + full heap). **The image is PRIVATE everywhere: Docker Hub `ticohq/switch-nvk-vulkan` → object-not-found + registry 401; ghcr.io/{dantiicu,ticohq}/* → 403.** ⇒ Running his app against OUR NVK = SAME `0xd5c` (the bug is the driver, not the app). No shortcut via his code — must solve the submit ourselves (prefix-scan above).

**⭐ `0xd5c` SOLVED via WARMUP RAMP; full pipeline runs on real Tegra; ONE blocker left = GPU write doesn't land (2026-05-26 cont):** The prefix-scan revealed the truth: submitting growing prefixes of NVK's pushbuf {16…1859 dw} made the full 1859-dw submit **succeed** (`kickoff=0x0`) and NVK proceeded to the next EXECs → **`vkCreateDevice→0` and the WHOLE smoke pipeline ran on HW** (`vkAllocateMemory/Map/CreateBuffer/Bind/CommandPool/Begin/End/QueueSubmit/QueueWaitIdle` all `→0`). ROOT CAUSE of `0xd5c`: **Tegra nvgpu's per-channel submit pool grows LAZILY with submit size** — NVK's first ~1859-dword init stream fails before the pool has grown; a single tiny prior submit (the EXEC probe, 3 dw) is NOT enough, but a size-ramp is. **FIX (committed): an inert WARMUP RAMP in `nouveau_channel_alloc`** — alloc a 32 KB internal BO, fill it with repeated `gen_fence_cmdlist` (syncpt-increment + flush ONLY, no 3D/compute engine state), and `AppendEntry`+`Kickoff` at sizes {32,128,512,1536,2048,4096,8192} dwords on the just-created channel (then free the BO). Proven on HW: `CHANNEL warmup ramp done (8190 dwords max)` → submit clean → device created. ⚠️ The prefix-replay of NVK's OWN pushbuf ALSO cleared `0xd5c` but **CORRUPTED the engine init** (partial init streams executed) → readback stayed zero for a 2nd reason; the warmup content MUST be inert. **REMOVED the prefix-scan; kept the content-peek (read-only) + EXEC probe (harmless).** ROBUSTNESS TODO for game ports: lazy-grow-to-max-seen means a submit bigger than the warmup max could still `0xd5c` — warm to ≥ any expected IB, or find the real channel-alloc param (libnx hardcodes `AllocGpfifoEx2 num_jobs=1`; we dropped BIT(8)/priv_cmd so num_jobs shouldn't matter — investigate what KickoffPb actually allocates).
**⭐ SUBMIT 0xd5c DEEP-DIVE (2026-05-26 late) — leak theory DISPROVEN; it's a size-proportional kernel kzalloc under LATE memory pressure:** After many failed fixes (warmup wedged the GPU via too many cache-flushes; incr-cmdlist-without-flush bit = INVALID = wedge+reboot; chunking = wrong per research; channel "prime" = doesn't persist), TWO audits clarified it. **Research:** 0xd5c = NvError 6 = kernel `-ENOMEM` (NOT -EAGAIN/Busy 14 = ring-full); libnx `AllocGpfifoEx2(fd,0x800,1,...)` the `1` is FLAGS not num_jobs (my "num_jobs=1" was a MISREAD); deko3d works w/o chunking (1 entry holds ~2M dwords). **Skeptical audit DISPROVED the VA-leak theory:** the 3-dword probe SUCCEEDS microseconds before the 1859 FAILS on the SAME channel (a leak would fail the probe too); 3 ok / 1859 fail same channel BUT 1920 ok on a FRESH channel → outcome tracks **submit SIZE**, not accumulated leak → it's a **per-submit, size-proportional kernel kzalloc** (priv-cmd/fence staging in gk20a_submit_prepare_syncs) that fails only LATE (after vkCreateDevice's full footprint + `__nx_heap_size=0` grabbing all memory). Selftest/probe pass because they run EARLY (low pressure) or are tiny. **Also: the shim's own "IncrFence causes 0xd5c" rationale is unsound (size-independent) and contradicted by the reference `libdrm_nouveau/pushbuf.c:225` which calls nvGpuChannelIncrFence every submit + works for GL on HW.** WORKFLOW THIS SESSION: upload to **`sdmc:/switch/nvk_smoke.nro`** (the user launches a FAVORITED copy there — NOT sdmc root! all earlier "stale binary" confusion was uploads going to root while launch used /switch/); stamp a **`[BUILD vN ...]` tag** in the smoke log header to confirm which binary ran; **`armDCacheFlush(cpu,size)` before readback** (GM20B not IO-coherent) is in the smoke test; va_len/4 is CORRECT (NVK passes bytes, verified vs nvkmd_nouveau_ctx.c). **Eden validates the CODE PATH only (no regression) — it FAKES the submit (accepts, no exec), so it can't reproduce 0xd5c.** **NEXT (v14, built+uploaded, UNRUN): decisive diagnostic** — on EXEC failure, submit a tiny + a 1857-dword VALID fence IB on NVK's SAME channel; if the valid-1857 ALSO fails → channel pressure (fix: cap `__nx_heap_size`, reduce nv-pool use); if it SUCCEEDS → NVK's pushbuf CONTENT is the trigger. Candidate fixes ranked: (1) cap `__nx_heap_size` to leave nv-service headroom, (2) mirror the reference: call IncrFence + use real fence + flush/NOP barrier, (3) num_inflight_jobs prealloc (needs manual channel setup, libnx hardcodes it).

**⭐⭐ vkCreateDevice WORKS + FULL PIPELINE RUNS ON HW; only the FINAL fill submit fails (2026-05-26 v17-v21):** Massive progress via reading FULL logs (the grep-slice hid it). THREE real fixes, each found from the complete log: **(1) NO_PREFETCH** — force `GPFIFO_ENTRY_NOT_MAIN|NO_PREFETCH` on every NVK push (NVK passes flags=0=MAIN+prefetch; deko3d sets NOT_MAIN always + NO_PREFETCH on CPU-built chunks). This made NVK's 1859-dword 3D init submit SUCCEED (`ioctl cmd=0x12 -> 0`). **(2) SKIP EMPTY EXEC** — NVK issues a push_count=0,wait=0,sig=0 EXEC right after init; our shim was force-submitting a spurious fence cmdlist for it → 0xd5c. Fix: empty EXEC = signal sigs from current fence + return 0, no submit. After this: **`D vkCreateDevice -> 0`** + vkCreateBuffer/AllocateMemory/Map/Bind/CommandPool/Buffers/Begin/End ALL → 0. **(3) drain-after-submit** — nvFenceWait on each submit's fence (proves the init's 3D EXECUTED: `EXEC drain chan=2 fence=27/1 rc=0x0`, channel healthy, NOT wedged). **REMAINING (the only blocker now): the FILL submit fails 0xd5c** — `vkQueueSubmit`'s EXEC (chan=2, push_count=1, a 28-dword fill pushbuf @0xc7fc5000, content `2003255d 00000001 c7ff7000 000003ff...` = method 0x55d subch1 referencing handle-4 VA 0x1c7ff7000, VALID/mapped). It's the **2nd real submit on chan 2** (init was 1st). Channel healthy (drain ok), content valid (mapped VA), NO_PREFETCH set, SMALLER than the init that passed → so it's a **per-submit kernel kzalloc failing under LATER memory state**. ROOT (deep RE): libnx `nvGpuChannelCreate` → `AllocGpfifoEx2(...,num_inflight_jobs=0,...)` so EVERY submit does a fresh kernel kzalloc(priv_cmd_entry/fence) (gk20a_submit_prepare_syncs); 1st submit (init, early/free mem) succeeds, 2nd (fill, later) fails ENOMEM. **deko3d avoids it by building its OWN channel (not libnx's) with prealloc.** **NEXT (UNDONE — needs user buy-in, risks current progress): build the GPU channel manually with `num_inflight_jobs>0`** (nvChannelCreate + BindChannel + raw `nvioctlChannel_AllocGpfifoEx2` with jobs>0 + AllocObjCtx + populate NvGpuChannel) instead of `nvGpuChannelCreate`. CAVEAT: libnx's AllocGpfifoEx2 param→struct mapping for num_inflight_jobs is unclear from headers (sig `(fd,num_entries,flags,unk0..3,fence)` vs kernel struct `{num_entries,num_inflight_jobs,flags,...}`); libnx source not in the toolchain image. Build tags: nacp version `0.NN.0-tag` shows in Sphaira (confirms binary); logs archived `D:\switch-nvk\winsys\logs\nvk_log_vNN.txt`. Current = v22.

**⭐⭐⭐ CRITICAL CORRECTION (2026-05-26 v23): `0xd5c` = `MAKERESULT(348, 6)` = `LibnxNvidiaError_Timeout` (`-ETIMEDOUT`), NOT InsufficientMemory!** (InsufficientMemory = desc 7 = `0xf5c`.) VERIFIED in libnx `result.h` enum (Timeout=6, InsufficientMemory=7) + arithmetic (348 | (6<<9) = 0xd5c). **ALL prior "0xd5c = ENOMEM / memory-pressure / num_inflight_jobs / kzalloc" reasoning in this file was based on a WRONG error reading — disregard it.** Timeout means the **channel was RESET**: nvgpu's submit guard does `if (c->has_timedout) return -ETIMEDOUT` for ALL submits once the channel faulted+recovered. So the real mechanism: **NVK's 3D init pushbuf kickoff is ACCEPTED (rc=0) but its EXECUTION FAULTS on the GPU** (MMU fault / GR illegal-method / PBDMA error) → nvservices runs channel recovery (`force_reset_ch`), latches `has_timedout` → every later submit (the empty exec is skipped by us; the fill; even our own trivial fence cmdlist = the v23 DIAG) returns `0xd5c` Timeout. The `EXEC drain rc=0x0` + `fence=27/402` were FALSE POSITIVES — recovery force-advances the syncpoint to max (`set_min_eq_max`) so our nvFenceWait completes even though the channel is dead. **So it is NOT memory/num_jobs/fill-content — it is: the 3D init FAULTS at execution.** NEXT: read the **error notifier** (`nvGpuChannelGetErrorNotification` → `NvNotification.info32` = NvNotificationType: 31=FifoErrorMmuErrFlt, 25=GrIllegalNotify, 32=PbdmaError, 8=FifoErrorIdleTimeout, 13=GrErrorSwNotify) after the init to learn the fault type+addr → fix the init's setup (likely an unmapped VA the init references, or a MAXWELL_B method/engine the channel isn't set up for). The devkitPro **Mesa 20 / libdrm_nouveau** (the proven GM20B GL stack, already cloned at `D:\switch-nvk\libdrm_nouveau` + devkitpro-mesa) is the reference for the CORRECT 3D channel setup/submission that doesn't fault — study it for what we're missing. (Research agents also clarified: libnx's `AllocGpfifoEx2` 2nd arg `1` IS `num_inflight_jobs=1` per Ryujinx's struct {num_entries,num_jobs,flags,...}; the kernel frees a submit's job/priv-cmd only from the host1x completion INTERRUPT via gk20a_channel_update, not from a userspace fence wait — relevant only if it were ENOMEM, which it is NOT.)

**⭐ v23-v26 — the blocker is a GR/FECS context-switch fault during NVK's 3D init (2026-05-26):** Progress fixes that STUCK: NO_PREFETCH (init kickoff accepted), skip-empty-EXEC (→ `vkCreateDevice=0`), standard IncrFence fence path, drain-after-submit, close-unmaps-VA, and a SetObject engine-bind prepend (3D=subch0..copy=subch4). The init kickoff is ACCEPTED but the GPU **MMU-faults executing it** (error NOTIFIER `info32=31`), the channel resets, all later submits Timeout `0xd5c`. Read the channel error info (`nvioctlChannel_GetErrorInfo`, NvError={type,info[31]}): **`type=2`=GR error (NOT mmu_error); info[0]=0x80000 = FECS-error bit (gr_intr bit19); info[1]=0x102310 = TRAPPED_ADDR → method 0x2310 subch0; info[3]=0x419e44 = trapped DATA; info[4]=0xb197 = 3D class.** Method **0x2310 = `NVB197_SET_FALCON04`** (the FECS microcontroller command interface). So NVK's 3D init issues a `SET_FALCON04` (data 0x419e44) to the **FECS (GR context-switch ucode)** and FECS errors → channel dead. This is a deep GR-context / context-switch-ucode interaction, NOT a stray buffer pointer. TWO leads remain (both via the local Mesa/libdrm_nouveau study): (1) **`vm_bind_op` MapBufferEx hardcodes small-page `0x1000` + kind=`op->flags&0xff`** — if NVK binds a region expecting big-page (0x20000) or a real tile kind and we map it wrong, the engine/FECS faults reading it (the known RESUME MapBufferEx risk; suspect: handle 5 = 128KB = one big-page); (2) the GR context / golden-ctx / instance-block set up by libnx's AllocObjCtx vs NVK's self-managed VA arena may conflict, OR NVK's SET_FALCON sequence needs the data at a low VA (0x419e44 ~4MB, BELOW our arena base 0x8000000) that nothing maps. **HONEST: this is the hard frontier (Maxwell GR/FECS bring-up — what Dan spent ~1.5 months on). vkCreateDevice + full pipeline WORK; the 3D engine EXECUTION (FECS context-switch) is the wall.** Build tags v23-v26 (nacp 0.NN.0). Logs in `winsys/logs/`.

**v22 UPDATE — custom channel RULED OUT; IncrFence didn't fix the fill; it's the 2nd submit on the 3D-init'd channel (2026-05-26):** Deep-RE was WRONG that deko3d builds its own channel — **deko3d USES libnx `nvGpuChannelCreate`** (same as us) + IncrFence + NOT_MAIN/NO_PREFETCH, and works with many submits. So NO custom channel (user correctly stopped that; Dan wouldn't either). Switched our hand-rolled in-stream fence cmdlist → standard **`nvGpuChannelIncrFence`** (the old "BIT(8)→0xd5c" was a DOUBLE increment: IncrFence + our cmdlist; cure = use exactly ONE = the standard). v22: init 1859 STILL succeeds (IncrFence single is fine, no regression) but the **FILL submit STILL fails 0xd5c** — same as v19/v20/v21. So the fill failure is NOT fence/prefetch/drain/channel-type. **KEY ISOLATING FACT: the selftest does 3 submits on its channel (T1/T2/T3) ALL rc=0x0, but the 2nd submit on NVK's channel 2 (the fill) fails.** So it's specifically **channel 2 AFTER NVK's 3D init EXECUTED** (drain proved it ran: syncpt 27→1). Leading hypothesis: NVK's 3D init execution (golden-ctx load / MME upload / 3D engine scratch) consumed GPU/kernel memory → the next submit's kzalloc fails ENOMEM. **DECISIVE UNKNOWN to test next (1 run): submit OUR fence cmdlist on chan 2 right BEFORE the real fill — if it ALSO fails → channel-2-post-3D-init state (memory); if it SUCCEEDS → the FILL's specific content/VA is the trigger.** Fill content peek: `2003255d 00000001 c7ff7000 000003ff 80002091 2003055d...` (method 0x55d subch1+subch0, refs handle-4 VA 0x1c7ff7000, mapped). Pipeline reaches vkQueueSubmit→-4 then exits. Everything else (instance/enumerate/device/buffer/mem/map/bind/cmdpool/begin/end) → 0.

**(SUPERSEDED) earlier blocker note: `vkCmdFillBuffer(0xCAFEBABE)` write does NOT land — readback all zeros**, with a CLEAN init (so NOT the prefix-corruption — warmup is inert and it still zeros). This is **GPU-write-visibility**: (1) **CPU/GPU cache coherency** — GM20B/Tegra is not IO-coherent; our BOs are CPU-*cacheable* `memalign` memory so Vulkan HOST_COHERENT is a lie → CPU readback hits a stale cache line and/or the GPU L2 wasn't flushed to the BO; (2) the completion fence may return BEFORE the GPU executed the fill. **NEXT (in progress): (a)** add a self-contained shim GPU-write test (write a sentinel to a BO via `SetReportSemaphore`/inline-to-memory, `nvFenceWait`, `armDCacheFlush`/invalidate, readback) to isolate execution-vs-coherency; **(b)** make host-visible BOs CPU-uncached (or `armDCacheFlush` the mapped range before readback) + ensure a GPU L2 flush after the submit. **Workflow: `nxlink -s` netloader is FLAKY (Stop-Job wedges it) → use FTP: `curl -T nvk_smoke.nro ftp://IP:5000/` + user launches via Sphaira file browser + poll `ftp://IP:5000/nvk_smoke.log` (clean exit returns to Sphaira).**
- **TEMP diagnostics still in the Mesa tree (NOT in the durable patch):** `vk_instance.c` (enumerate-path + &drmGetDevices2 logging via `g_drm_shim_log_sink`), `nvkmd_nouveau_va.c` (`shim_va_log` on VA-heap exhaustion). Revert before a clean re-extract. Real Mesa patches to fold: `util/libdrm.h` (HAVE_LIBDRM on `__SWITCH__`) + `nvkmd_nouveau_dev.c` (arena heap override) + the meson `vk_drm_syncobj.c` one. `drm_shim.c` builds with `-DDRM_SHIM_DEBUG` for the trace; the smoke `.nro` sets the `g_drm_shim_log_sink` so all shim+runtime traces land in `sdmc:/nvk_smoke.log`.

**M2-WINSYS IMPLEMENTED (structural, compile+link-clean; HW-validation pending) (2026-05-26):** the M1 no-op stubs in `winsys/drm_shim.c` are now real GPU code, built from the libnx nv API (headers under `/opt/devkitpro/libnx/include/switch/nvidia/`) + devkitPro `libdrm_nouveau/source/pushbuf.c` as the reference (it shows the exact `nvGpuChannelCreate/AppendEntry/IncrFence/Kickoff/GetFence` GPFIFO path + the Maxwell fence-incr & cache-flush cmdlists, copied verbatim). KEY discovery: NVK uses the NEW nouveau uAPI (explicit VA: it owns the VA allocator, `VM_BIND` maps a BO sub-range at an NVK-chosen VA, `EXEC` pushes IBs by VA), NOT libdrm_nouveau's old GEM_PUSHBUF. **VM_BIND** → `nvioctlNvhostAsGpu_AllocSpace`(reserve fixed VA) + `…_MapBufferEx`(FixedOffset, buffer_offset=bo_offset, mapping_size=range) — the RAW ioctl is public and supports sub-ranges, unlike the libnx `nvAddressSpaceMapFixed` wrapper which hardcodes offset/size=0 (whole-buffer). **CHANNEL_ALLOC**→real `NvGpuChannel`+Zcull+builtin-cmdbuf(fence/flush cmdlists); **EXEC**→append pushes+fence cmdlist+Kickoff+stash `NvFence` into sig syncobjs; **drmSyncobjWait**→`nvFenceWait` (snapshot fence under lock, wait OUTSIDE lock to avoid stalling submitters; abs-ns→rel-µs). Compile-clean `-Wall -Wextra`, EXE still links (exit 0). **NOT RUN YET. KNOWN HW-VALIDATION RISKS (only settleable on real Tegra):** (1) `MapBufferEx` page_size — we pass the AS big-page (64KB) but NVK aligns to 4KB → small-page binds may need page_size=0x1000; (2) **subchannel/engine binding** — NVK may rely on kernel subchannel alloc via NVIF NEW (which our shim only acks); if so we must emit Maxwell `SetObject` into the channel stream (deko3d does this); (3) we CPU-serialize wait-syncs instead of GPU-side semaphore waits; (4) `gem_cpu_prep` is a no-op (no per-BO fence map). **HEADLESS SMOKE `.nro` BUILT (2026-05-26, UNRUN):** `winsys/smoke/nvk_smoke.c` + `winsys/build-nro.sh` → **`D:\switch-nvk\nvk_smoke.nro`** (12.6MB, full Application: `__nx_applet_type=AppletType_Application` + `__nx_heap_size=0`). Loaderless ICD (resolves everything via `vk_icdGetInstanceProcAddr` — libnvk.a exports ONLY that, NOT vkCreateInstance; so use `VK_NO_PROTOTYPES`), staged A–I (instance→enumerate GM20B→qfam→device→host-vis mem+map→buffer+bind→`vkCmdFillBuffer(0xCAFEBABE)`→QueueSubmit+WaitIdle→readback verify), logs each step+VkResult to `sdmc:/nvk_smoke.log` (flushed/crash-safe) + stdout (live via `nxlink -s`). Sets `NVK_I_WANT_A_BROKEN_VULKAN_DRIVER=1`+`MESA_SHADER_CACHE_DISABLE=1` in-code. **RUN: launch via Sphaira "boot as application" (NOT album applet), then read `sdmc:/nvk_smoke.log` — the last stage letter reached pinpoints the broken layer.** Then M3 WSI/triangle. State: `D:\switch-nvk\RESUME_NVK.md`.

**M1-WINSYS DONE — THE WHOLE NVK DRIVER LINKS INTO A SWITCH EXE (2026-05-26):** a strict EXE link (`D:\switch-nvk\winsys\build-exe-linktest.sh`: whole-archived `libnvk.a` + all 21 static libs + the winsys shim + the libc shim + a real `main`, via devkitA64 `switch.specs` = crt0+`switch.ld`, linked with `g++` for libstdc++ since nv50_ir codegen is C++, `-Wl,--wrap=open,--wrap=close`, NO `--gc-sections`) **succeeds with ZERO unresolved symbols → 12 MB `.elf`**. Three pieces landed this session (all committed, reproducible): (1) `winsys/drm_shim.c`+`.h` (all `drm*` over libnx nv, see below); (2) `winsys/switch_libc_shim.c` = the `open`/`close` wrap (routes `/dev/dri/renderD128`→`drm_shim_open`, BO `mmap(fd,map_handle)`→`drm_shim_mmap`) + ALL group-C libc/std gaps (getrandom→`randomGet`, posix_memalign, pread/pwrite via lseek, flock/pthread_sigmask no-op, sysconf Tegra defaults, regcomp=0/regexec=REG_NOMATCH/regfree, pipe/mkfifo/dirfd/fstatat, getpwuid_r→notfound, chown family + getuid family→root/0) — `mmap`/`munmap` moved here from compat.c; (3) Mesa patch `src/vulkan/runtime/meson.build`: compile `vk_drm_syncobj.c` on `host_machine.system()=='horizon'` (gives `vk_drm_syncobj_get_type/_finish`; our shim supplies its `drmSyncobj*`+`drmGetCap`) — now the 7th file in `patches/switch-nvk-mesa-25.0.7.patch` (round-trip verified). GOTCHAS: `strrchr`/`secure_getenv` etc. in the `.so` undefined-list were NOISE (the `.so` doesn't link `-lc`); the `.so` link is a DEAD END (shared object can't take crt0 — must link an EXE). `git` in Bash is hijacked by the rtk hook (`rtk git diff` returns a token-SUMMARY, not a real patch — it silently truncated the patch file once); **do all git via PowerShell or `rtk proxy git`.** **M1 EXE = link harness only (main just touches the ICD entry); M2 (real VM_BIND→nvAddressSpaceMapFixed + EXEC→GPFIFO + nvFence-backed syncobj) is required before anything renders. Runtime needs `NVK_I_WANT_A_BROKEN_VULKAN_DRIVER=1` (GM20B is SOC/non-conformant).** READ `D:\switch-nvk\RESUME_NVK.md`.

**M1-WINSYS STARTED — `drm_shim.c` written + compile-clean (2026-05-26):** `D:\switch-nvk\winsys\drm_shim.c` (+`drm_shim.h`) implements ALL group-A `drm*` link-TODO symbols over libnx `nv`, cross-compiles clean with devkitA64 `-Wall -Wextra` (verify: `docker run --rm -v "D:\switch-nvk:/work" -w /work switch-nvk-build bash winsys/compile-test.sh`), undefined refs = ONLY libc(`-lc`)+libnx nv(`-lnx`). Architecture confirmed by reading the real call-sites: modern NVK (`src/nouveau/winsys/*` + `nvkmd/nouveau/*`) drives the GPU via libdrm CORE calls carrying nouveau uAPI structs — `drmCommandWrite[Read](fd, DRM_NOUVEAU_{GETPARAM,NVIF,VM_INIT,VM_BIND,EXEC,GEM_NEW/INFO/CPU_PREP,CHANNEL_ALLOC/FREE}, &struct, size)` — NOT the old `libdrm_nouveau` `nouveau_*` API (that repo is the libnx-nv MAPPING reference only). Shim reports a **synthetic GM20B**: NVIF device-INFO → chipset 0x12b/SOC/Maxwell; NVIF SCLASS → hardcoded Maxwell-B class set (eng3d=`MAXWELL_B` 0xb197 passes NVK's `>=KEPLER_A` gate; +0x902d/0xb0b5/0xb1c0/0xa140); GETPARAM GRAPH_UNITS=(2<<8)|1; VM_INIT→0 (sets `has_vm_bind`, else NVK rejects "needs kernel ≥6.6"). GEM_NEW/INFO/CPU_PREP + `drmCloseBufferHandle` are REAL (libnx nvmap + memalign, mirrors `libdrm_nouveau` nouveau_bo_new); syncobjs = software table (binary; no-op-success Wait since EXEC stubbed); **VM_BIND + EXEC = no-op-success stubs (M2)**; PRIME/sync_file = ENOSYS. `drm_shim.h` exports `drm_shim_{open,close,owns_fd,is_render_node,mmap,munmap}` for the M2 `open()/mmap()` libc wrap (synthetic fd `0x6E760000`, render node `/dev/dri/renderD128`). **2 non-`drm*` items still block the M1 link:** (1) compile Mesa `src/vulkan/runtime/vk_drm_syncobj.c` → `vk_drm_syncobj_get_type/_finish` (consumes our `drmSyncobj*`+`drmGetCap`); (2) the `open/close/mmap/munmap` libc wrap (`switch_libc_shim.c`) + EXE link with `switch.ld`. NVK signature gotcha: `drmSyncobjQuery` takes NON-const `uint32_t*` (Reset/Signal/TimelineSignal are const).

**M1 CROSS-CONFIGURE CLEAN (2026-05-25):** `meson setup` of **Mesa 25.0.7** NVK for the Switch SUCCEEDS (`vulkan-drivers=nouveau`, `mesa-clc=enabled`, `static-libclc=all`, NAK Rust compiler + `paste`/proc-macro subprojects accepted via our Rust-for-Switch toolchain). All in `D:\switch-nvk\`: `configure-mesa.sh` (base cross-file from `meson-toolchain.sh switch` + `/tmp/rust.cross` adding `rust=['/work/rustc-switch.sh']` & bindgen + a NATIVE x86 `--native-file` for the host tool `mesa_clc`'s deps). **The dep chain that had to be solved (order hit):** mesa_clc needs LLVM≥15 (`-Dllvm=auto`, llvm-config-15) → llvmspirvlib (`libllvmspirvlib-15-dev`) → SPIRV-Tools (`spirv-tools` pkg) → libdl (Switch has none → empty `libdl.a` stub via `aarch64-none-elf-ar rcs`) → CLC dynamic-libclc wants `mmap` (Switch newlib lacks → `-Dstatic-libclc=all` embeds it instead) → static path needs `/usr/lib/clc/spirv-mesa3d-.spv` which is in the **RUNTIME `libclc-15` pkg, NOT `libclc-15-dev`** (the trap: -dev has only headers+.pc) → NIL needs **`cbindgen`** (cargo, Rust→C headers) alongside bindgen. The native-tool .pc's (libclc/LLVMSPIRVLib/SPIRV-Tools) are copied into the Switch **cross** pkgconfig dir so cross-lookup finds them (they carry x86 paths — correct, mesa_clc runs on the build host). All baked into the `switch-nvk-build` Dockerfile. NEXT = `ninja -C /work/mb` (persistent build dir = `D:\switch-nvk\mb`) → the C-side Switch source patches (Dan's version-by-version grind), then the M1 winsys shim (DRM-nouveau ioctls → libnx `nv`, ref `D:\switch-nvk\libdrm_nouveau`).

Contrast: **Dawn-GL** (the other path, no NVK) ALREADY runs Aurora's full renderer on Switch — see
[[dawn-over-gl-already-patched]]. Slower (abstraction + runtime shader compile) but needs no driver.
Vulkan/NVK is the performance/"proper" path gated on the driver. See [[mesa-fork-distribution-network]].
